import { View, Text } from "react-native";

const Menu = () => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text style={{ fontWeight: "bold", fontSize: 20 }}>Hello Menu</Text>
    </View>
  );
};

export default Menu;
